<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/blocking_progress_container"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="vertical"
    android:paddingStart="@dimen/big_margin"
    android:paddingTop="@dimen/big_margin"
    android:paddingEnd="@dimen/big_margin"
    android:paddingBottom="@dimen/big_margin">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:gravity="center_vertical">

        <ProgressBar
            android:id="@+id/blocking_progress_spinner"
            style="?android:attr/progressBarStyleSmall"
            android:layout_width="32dp"
            android:layout_height="32dp"
            android:layout_marginEnd="@dimen/activity_margin"
            android:indeterminate="true" />

        <TextView
            android:id="@+id/blocking_progress_message_line1"
            android:layout_width="0dp"
            android:layout_height="@dimen/blocking_progress_line_height"
            android:layout_weight="1"
            android:maxLines="1"
            android:ellipsize="end"
            android:gravity="center_vertical"
            android:textSize="@dimen/bigger_text_size" />
    </LinearLayout>

    <TextView
        android:id="@+id/blocking_progress_message_line2"
        android:layout_width="match_parent"
        android:layout_height="@dimen/blocking_progress_line_height"
        android:layout_marginTop="@dimen/small_margin"
        android:gravity="center_vertical|end"
        android:textSize="@dimen/bigger_text_size" />

    <com.google.android.material.button.MaterialButton
        android:id="@+id/blocking_progress_cancel"
        style="@style/Widget.Material3.Button.TextButton.Dialog"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="end"
        android:layout_marginTop="@dimen/activity_margin"
        android:text="@string/cancel"
        android:visibility="gone" />
</LinearLayout>
